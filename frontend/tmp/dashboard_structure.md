# 🎯 Структура личного кабинета

## 📁 Структура файлов

```
src/
├── views/
│   ├── dashboard/
│   │   ├── DashboardView.vue              # Главная страница ЛК
│   │   ├── AccountSettingsView.vue        # Настройки аккаунта
│   │   ├── MyOrganizersView.vue          # Список организаторов
│   │   ├── CreateOrganizerView.vue       # Создание организатора
│   │   ├── EditOrganizerView.vue         # Редактирование организатора
│   │   ├── OrganizerEventsView.vue       # События организатора
│   │   ├── CreateEventView.vue           # Создание события
│   │   └── EditEventView.vue             # Редактирование события
│
├── components/
│   ├── dashboard/
│   │   ├── DashboardLayout.vue           # Layout для ЛК
│   │   ├── DashboardSidebar.vue          # Боковое меню
│   │   ├── DashboardHeader.vue           # Шапка ЛК
│   │   ├── Breadcrumbs.vue               # Хлебные крошки
│   │   │
│   │   ├── account/
│   │   │   ├── AccountCard.vue           # Карточка аккаунта
│   │   │   ├── AccountEditForm.vue       # Форма редактирования
│   │   │   └── DeleteAccountModal.vue    # Модалка удаления
│   │   │
│   │   ├── organizers/
│   │   │   ├── OrganizersList.vue        # Список организаторов
│   │   │   ├── OrganizerCard.vue         # Карточка организатора
│   │   │   ├── OrganizerForm.vue         # Форма организатора (уже есть)
│   │   │   ├── DeleteOrganizerModal.vue  # Модалка удаления
│   │   │   └── EmptyOrganizers.vue       # Пустой список
│   │   │
│   │   ├── events/
│   │   │   ├── EventsList.vue            # Список событий
│   │   │   ├── EventCard.vue             # Карточка события
│   │   │   ├── EventForm.vue             # Форма события
│   │   │   ├── DeleteEventModal.vue      # Модалка удаления
│   │   │   ├── EmptyEvents.vue           # Пустой список
│   │   │   └── EventFilters.vue          # Фильтры событий
│   │   │
│   │   └── shared/
│   │       ├── StatusBadge.vue           # Бейдж статуса
│   │       ├── SuccessModal.vue          # Модалка успеха
│   │       ├── ConfirmModal.vue          # Модалка подтверждения
│   │       └── StatsCard.vue             # Карточка статистики
│
├── stores/
│   ├── dashboardStore.js                 # Store для ЛК
│   └── userOrganizersStore.js           # Store организаторов юзера
│
└── router/
    └── dashboard.js                      # Роуты ЛК
```

---

## 🗺️ Роутинг

```javascript
// router/dashboard.js
{
  path: '/dashboard',
  component: DashboardLayout,
  meta: { requiresAuth: true },
  children: [
    {
      path: '',
      name: 'dashboard',
      component: DashboardView
    },
    {
      path: 'account',
      name: 'dashboard-account',
      component: AccountSettingsView
    },
    {
      path: 'organizers',
      name: 'dashboard-organizers',
      component: MyOrganizersView
    },
    {
      path: 'organizers/create',
      name: 'dashboard-organizers-create',
      component: CreateOrganizerView
    },
    {
      path: 'organizers/:slug/edit',
      name: 'dashboard-organizers-edit',
      component: EditOrganizerView
    },
    {
      path: 'organizers/:slug/events',
      name: 'dashboard-organizer-events',
      component: OrganizerEventsView
    },
    {
      path: 'organizers/:organizerSlug/events/create',
      name: 'dashboard-events-create',
      component: CreateEventView
    },
    {
      path: 'organizers/:organizerSlug/events/:eventSlug/edit',
      name: 'dashboard-events-edit',
      component: EditEventView
    }
  ]
}
```

---

## 🎨 Дизайн система

### Статусы

| Статус | Цвет | Описание |
|--------|------|----------|
| **draft** | `--color-gray-light` | Черновик |
| **pending** | `--color-warning` | На модерации |
| **published** | `--color-success` | Опубликовано |
| **rejected** | `--color-error` | Отклонено |
| **cancelled** | `--color-gray-dark` | Отменено |

### Компоненты UI

```vue
<!-- StatusBadge.vue -->
<span class="status-badge" :class="`status-${status}`">
  {{ statusText }}
</span>
```

---

## 🔄 Поток работы пользователя

### 1. Вход в личный кабинет
```
/dashboard
├── Статистика (события, организаторы, просмотры)
├── Последние события
└── Быстрые действия
```

### 2. Управление организаторами
```
/dashboard/organizers
├── Список организаторов
│   ├── [Активный] Organizer 1
│   │   ├── 📊 Статистика
│   │   ├── 👁️ Просмотр событий
│   │   ├── ✏️ Редактировать
│   │   ├── ➕ Добавить событие
│   │   └── 🗑️ Удалить
│   └── [На модерации] Organizer 2
└── ➕ Создать организатора
```

### 3. Управление событиями
```
/dashboard/organizers/{slug}/events
├── Breadcrumbs: Dashboard > Мои организаторы > [Organizer Name]
├── Фильтры (Все / Активные / На модерации / Черновики)
├── Список событий
│   └── Event Card
│       ├── Статус
│       ├── Дата
│       ├── Просмотры
│       ├── 👁️ Посмотреть на сайте
│       ├── ✏️ Редактировать
│       └── 🗑️ Удалить
└── ➕ Добавить событие
```

---

## 📊 Состояния Store

### dashboardStore.js
```javascript
{
  user: {
    id, username, email, 
    avatar, stats: { 
      organizers, events, views 
    }
  },
  organizers: [],
  selectedOrganizer: null,
  events: [],
  loading: false,
  modals: {
    showSuccess: false,
    showConfirm: false,
    successMessage: '',
    confirmAction: null
  }
}
```

---

## 🎯 Ключевые взаимодействия

### A. Создание организатора

1. **Пользователь** → `/dashboard/organizers/create`
2. **Заполняет** → `OrganizerForm`
3. **Отправка** → `organizersAPI.create()`
4. **Успех** → `SuccessModal` ("На модерации")
5. **Редирект** → `/dashboard/organizers`
6. **Обновление** → Список организаторов (статус: pending)

### B. Создание события

1. **Пользователь** → Выбирает организатора
2. **Клик** → "Добавить событие"
3. **Переход** → `/dashboard/organizers/{slug}/events/create`
4. **Заполняет** → `EventForm`
5. **Отправка** → `activitiesAPI.create()`
6. **Успех** → `SuccessModal` ("На модерации")
7. **Редирект** → `/dashboard/organizers/{slug}/events`
8. **Обновление** → Список событий (статус: pending)

### C. Редактирование

1. **Клик** → Кнопка "Редактировать"
2. **Переход** → Edit View
3. **Загрузка** → Данных в форму
4. **Изменение** → Поля
5. **Сохранение** → `API.update()`
6. **Успех** → Уведомление
7. **Возврат** → К списку

### D. Удаление

1. **Клик** → Кнопка "Удалить"
2. **Модалка** → `ConfirmModal` с предупреждением
3. **Подтверждение** → Кнопка "Удалить"
4. **Отправка** → `API.delete()`
5. **Успех** → Удаление из списка
6. **Уведомление** → Успешное удаление

---

## 🎨 UI/UX особенности

### 1. Индикаторы статуса
- **Цветные бейджи** для визуального различия
- **Иконки** для быстрого распознавания
- **Тултипы** с пояснениями

### 2. Пустые состояния
- **Дружелюбные иллюстрации**
- **Понятные CTA кнопки**
- **Подсказки** как начать работу

### 3. Модерация
- **Ясные сообщения** о статусе модерации
- **Таймлайн** (опционально) "Отправлено X дней назад"
- **Причина отклонения** если статус rejected

### 4. Быстрые действия
- **Дублировать событие** (для повторяющихся)
- **Быстрое редактирование** (например, только даты)
- **Batch actions** (массовые операции)

---

## 🔐 Права доступа

### Ограничения
- Редактировать/удалять могут **только авторы**
- События **привязаны к организатору**
- Нельзя создать событие **без организатора**
- Нельзя удалить организатора **с активными событиями**

### Проверки на фронте
```javascript
const canEdit = (item) => {
  return item.author.id === currentUser.id;
};

const canDelete = (organizer) => {
  return organizer.events_count === 0;
};
```

---

## 📱 Адаптивность

### Desktop (> 1024px)
- Sidebar слева
- Контент справа
- 2-3 колонки для карточек

### Tablet (768px - 1024px)
- Sidebar скрывается в меню
- 2 колонки для карточек

### Mobile (< 768px)
- Гамбургер меню
- 1 колонка
- Упрощенные карточки
- Bottom navigation (опционально)

---

## 🎯 Приоритеты разработки

### Phase 1 (MVP)
1. ✅ Layout и навигация
2. ✅ Список организаторов
3. ✅ Создание организатора
4. ✅ Список событий
5. ✅ Создание события

### Phase 2
1. ✅ Редактирование
2. ✅ Удаление
3. ✅ Статусы и модерация
4. ✅ Модальные окна

### Phase 3
1. ✅ Статистика
2. ✅ Фильтры
3. ✅ Поиск
4. ✅ Черновики

---

## 🎨 Следующий шаг

Создадим компоненты в следующем порядке:

1. **DashboardLayout** - основной layout
2. **OrganizersList** - список организаторов
3. **OrganizerCard** - карточка организатора
4. **EventsList** - список событий
5. **EventCard** - карточка события
6. **Модальные окна** - Success, Confirm
7. **Формы** - Event form

Готовы начать? 🚀